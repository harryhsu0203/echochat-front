// 這個檔案的目的是觸發 Render 重新部署
// 每次修改這個檔案並推送，Render 就會重新啟動服務器

const triggerTime = new Date().toISOString();
console.log(`🚀 觸發 Render 重新啟動: ${triggerTime}`);

// 這會強制 Render 檢測到變更並重新部署
// 新的部署會執行 connectDatabase() 函數
// 該函數會強制重設所有帳號密碼為 admin123

module.exports = {
    triggerTime,
    message: '強制重新啟動 Render 服務器以修復登入問題'
};